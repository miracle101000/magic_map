name: magic_map
description: Path-based, typed access to nested Dart maps and lists - safe deep reads, deep writes, glob queries, immutable updates and JSON helpers, plus optional dot access.
version: 2.1.0
homepage: https://github.com/miracle101000/magic_map
repository: https://github.com/miracle101000/magic_map
topics:
  - json
  - map
  - path
  - nested-data
  - config

environment:
  sdk: ^3.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
